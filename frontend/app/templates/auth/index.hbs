<div class="card">
  <div class="card-header">
    JSONAPI Schema Generator
  </div>
  <div class="card-block">
    <h4 class="card-title">Welcome, {{sessionWrapper.user.name}}</h4>
    {{#link-to "auth.contracts" class="btn btn-primary"}}My Service contracts{{/link-to}}
  </div>
</div>